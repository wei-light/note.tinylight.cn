const Footer = () => (
  <footer className="pb-8 pt-12 px-6 w-full max-w-2xl mx-auto text-sm opacity-30">
    <span>{'Tiny Light'}</span>
    <span>{' © '}</span>
    <span>{`${new Date().getFullYear()}`}</span>
  </footer>
)

export default Footer
